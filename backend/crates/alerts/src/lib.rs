//! `alerts` — monitoring rule evaluation and delivery (Phase 4).
//!
//! Evaluates alert rules against ingested on-chain activity, dedupes firings,
//! and delivers notifications through configured destinations (webhook/email).
//!
//! **Status:** Phase 4 skeleton. No live code yet — Phase 0 only requires the
//! workspace member to exist so the crate split is stable from day one.

pub fn placeholder() -> &'static str {
    "alerts: phase 4 skeleton"
}
